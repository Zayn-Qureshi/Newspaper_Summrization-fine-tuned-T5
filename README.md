📰 NewsDigest — AI Article Summarizer

A lightweight and efficient T5-based text summarization web app powered by HuggingFace Transformers, Flask, and your fine-tuned T5 model.
This project is deployed on Hugging Face Spaces and includes both the model and the web interface.

🚀 Live Demo

🔗 Hugging Face Space: Add your link here
Example:

https://huggingface.co/spaces/<your-username>/<your-space-name>

📌 Features

✅ Real-time text summarization using your fine-tuned T5 model

✅ Clean and responsive UI (NewsDigest design)

✅ API-friendly Flask backend

✅ Deployed on Hugging Face Spaces

✅ Supports article text, PDFs, and long-form content

✅ Fully open-source and easy to customize

🧠 Model

This project uses a fine-tuned T5 model that you trained on your dataset.

Model repo on Hugging Face:

https://huggingface.co/<your-username>/<your-model-name>


Structure inside your model folder:

config.json
pytorch_model.bin
tokenizer_config.json
special_tokens_map.json
spiece.model (if using SentencePiece)

🏗️ Project Structure
├── app.py                  # Flask backend
├── requirements.txt        # Dependencies
├── static/                 # CSS / JS files
├── templates/
│   └── index.html          # UI template
├── fine_tuned_t5/          # Your local model (optional for HF Spaces)
└── README.md

📦 Installation (Local Development)
1️⃣ Clone the repo
git clone https://github.com/<your-username>/<your-repo>.git
cd <your-repo>

2️⃣ Create a virtual environment
python -m venv venv
source venv/bin/activate   # Linux / Mac
venv\Scripts\activate      # Windows

3️⃣ Install dependencies
pip install -r requirements.txt

4️⃣ Run Flask app
python app.py


App runs on:

https://huggingface.co/spaces/Zain78877/news-summarization-demo

🧪 API Usage
POST /summarize
curl -X POST http://127.0.0.1:5000/summarize \
     -H "Content-Type: application/json" \
     -d '{"text": "Your long text here"}'

Response:
{
  "summary": "Short summary generated by the model."
}

🛠️ Technology Stack

Python

Flask

Transformers (HuggingFace)

T5 (fine-tuned version)

HTML / CSS / JS

Hugging Face Spaces

🚀 Deployment on Hugging Face Spaces

Upload:

app.py

requirements.txt

templates/

static/

Set Space type to:

Static + Python (Gradio / Streamlit / Custom Flask)


Add your fine-tuned model in:

/models


Add this to your app.py:

model_path = "./fine_tuned_t5"
tokenizer = T5Tokenizer.from_pretrained(model_path)
model = T5ForConditionalGeneration.from_pretrained(model_path)
📝 License

This project is open-source under the MIT License.

